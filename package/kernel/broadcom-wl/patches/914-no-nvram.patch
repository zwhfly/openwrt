--- a/driver/nvram_stub.c
+++ b/driver/nvram_stub.c
@@ -35,7 +35,7 @@
 	vars_t *new;
 
 	nvram_init_done++;
-	if (nvram_init_done != 1)
+	//if (nvram_init_done != 1)
 		return 0;
 
 	osh = si_osh(sih);
